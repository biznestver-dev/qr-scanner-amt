# Настройка доступа

## Google Apps Script

1. В настройках проекта Apps Script добавьте Script Properties:
   - `OWNER_EMAIL` — `biznestver@gmail.com` (владелец приложения, всегда `admin`).
   - `ADMIN_EMAILS` — email администраторов через запятую.
   - `MANAGER_EMAILS` — email менеджеров через запятую.
   - `VIEWER_EMAILS` — email пользователей только для чтения через запятую.
   - `BOT_SHARED_SECRET` — длинная случайная строка для запросов Telegram-бота.
2. Опубликуйте Web App с настройками:
   - **Execute as:** User accessing the web app.
   - **Who has access:** только пользователи вашей Google Workspace организации.
3. После изменения `GoogleScript.gs` создайте новую версию и обновите deployment.
4. Один раз запустите в редакторе Apps Script функцию `createDailyOverdueTrigger`, чтобы удалить старый автоматический триггер. Новые автоматические триггеры больше не создаются.

## Telegram-бот

Добавьте тот же `BOT_SHARED_SECRET` в `.env` рядом с `BOT_TOKEN`:

```env
BOT_SHARED_SECRET=replace-with-a-long-random-value
```

После этого перезапустите бота. Команда `/start` регистрирует или обновляет чат в листе `Пользователи`; повторные запуски не создают дубликаты. Бот не обрабатывает инвентарные номера, QR-коды, справочники или команды управления.

Автоматическая рассылка отключена. Напоминание отправляется только вручную кнопкой «Должники» пользователем с ролью `manager` или `admin`.

Не добавляйте `.env` в Git и отзовите ранее раскрытый `BOT_TOKEN` через BotFather.

Без настроенных email-списков приложение работает только в режиме чтения, а серверные изменения отклоняются.
Владелец из `OWNER_EMAIL` имеет полный доступ даже без добавления в `ADMIN_EMAILS` и должен лично управлять остальными списками доступа.